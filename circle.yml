machine:
  python:
    version: 3.4.1

dependencies:
  cache_directories:
  - "/home/ubuntu/nvm"

  override:
    - pip install --exists-action w -r requirements.txt


test:
  override:
  - pip freeze
  - python CRUDFilter/manage.py test

